dist: focal
language: perl
perl:
  - "5.30"
  - "5.24"
  - "5.12"
before_install:
  - sudo cpanm --notest Module::Build
install:
  - sudo cpanm --quiet --installdeps --notest .
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - cpanminus
    - gzip
    - lbzip2
    - lz4
    - lzip
    - pbzip2
    - pigz
    - plzip
    - xz-utils
    - zstd
